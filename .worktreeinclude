# Local environment files to copy into worktrees
# These are gitignored, so they're missing from fresh worktree checkouts
# .worktreeinclude uses .gitignore syntax — only gitignored files that match are copied

# Claude Code local config
.claude/.env

# Python environment
.env
.envrc
